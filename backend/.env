# === CORS: where your frontend runs (comma-separated if multiple) ===
# For local dev with Vite:
FRONTEND_ORIGIN=http://localhost:5173

# === Quantum RNG source (optional) ===
# Leave empty to use secure OS randomness (os.urandom).
# Example if you have a provider:
# QRNG_URL=https://your-qrng.example/api/bytes?length=
# QRNG_HEADER=Authorization:Bearer YOUR_TOKEN
QRNG_URL=
QRNG_HEADER=

# === Server-side Firestore (optional) ===
# Your app already works with frontend writing to Firestore directly.
# Only set this to true if you want the Python backend to write messages.
FIREBASE_SERVER_WRITES=false

# If FIREBASE_SERVER_WRITES=true, provide a Google service account JSON:
# Absolute path to the service account file (Windows example: C:\keys\svc.json)
GOOGLE_APPLICATION_CREDENTIALS=
# Optional if your service account JSON already encodes the project:
GCP_PROJECT_ID=
